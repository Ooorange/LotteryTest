<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content" >

    <ImageView
        android:id="@+id/speaker"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:src="@drawable/speak_switcher_height" />

    <TextView
        android:id="@+id/wordToSpeak"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/speaker"
        android:layout_centerHorizontal="true"
        android:layout_toRightOf="@+id/speaker"
        android:background="@drawable/content_9"
        android:maxEms="15"
        android:textColor="@drawable/DarkBlue" />

</RelativeLayout>